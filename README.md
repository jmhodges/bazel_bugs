Run `mkdir -p .vscode && mv vscode-settings.json .vscode/settings.json`

Then open a foo/foo.go and watch the lower bar for an error notification (and,
on my machine, for the ./bazel_bugs/external/ directory to disappear).
