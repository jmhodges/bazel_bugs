load(
    "@io_bazel_rules_docker//container:container.bzl",
    "container_image",
)

container_image(
    name = "dockertest",
    base = "@mysql//image",
    env = {"MYSQL_ALLOW_EMPTY_PASSWORD": "1"},
)
